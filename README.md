K-nearsest-neighbors is a school's exercice. It can be build using ``xmake run main``. Tests have been implemented to ensure the validity of my implementation of both a binary heap and a kd-tree, and can be run using ``xmake run tests``.
Despite my efforts, the kd-tree is fairly slow, I learn a bit to late that they are useless in high dimension...
